
class HRSystemModel { //model; 資料匯入地方
    constructor (data){
      this.data = data
    }
    getData() { //這裡是要撈 假設的 高中低資料
        return this.data
    }
}
export default HRSystemModel  //只有一個所以要寫default
